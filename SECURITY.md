# 安全与隐私说明（Security & Privacy）

DupFinder 是一个**纯本地、离线**运行的 macOS 重复文件清理工具。本文档说明它的隐私边界与安全设计，
供使用者与潜在贡献者参考。

## 隐私立场（Privacy）

- **零网络访问**：应用不含任何网络请求（无 `URLSession`、无 socket、无 `curl`、无外部 API 调用）。
  它不会把文件名、文件内容、哈希结果或你的任何使用行为发送到任何服务器。
- **零遥测 / 零分析 SDK**：项目不引入任何第三方依赖（见 `Package.swift`），不使用崩溃上报、
  行为分析、广告或追踪 SDK。
- **仅访问你主动选择的目录**：扫描范围严格限定为用户通过文件选择面板添加的文件夹，
  不扫描整个磁盘，也不会读取你未指定的位置。
- **文件内容仅用于本地计算**：读取文件内容只为两件事——计算 SHA-256（精确去重）和提取
  Apple Vision 视觉特征（相似图片检测）。这些计算全部在本地完成，结果不离开你的机器。

## 持久化到磁盘的数据（On-disk data）

唯一会写入磁盘的衍生数据是**哈希缓存**，位于：

```
~/Library/Caches/DupFinder/hashcache.json
```

- **内容**：`[ 缓存键 : 内容哈希 ]` 的字典。
  - 缓存键 = `设备号:inode:文件大小:修改时间`——**不含文件路径，也不含文件名**。
  - 值为文件内容的 SHA-256。
- **隐私含义**：即使他人取得该缓存文件，也无法得知你扫描过哪些文件或它们的名字；
  只能知道「某些文件的内容哈希」。该文件仅存于本机，且可随时在 App 内「清空哈希缓存」删除。
- **缓存会随使用累积增长**（按 inode 累计、只增不删），但提供了一键清理入口。
- 删除操作（`⌘Z` 撤销所需的「原路径 → 废纸篓路径」映射）仅保存在**内存**中，
  **不会**写入磁盘——退出 App 后即消失，不留下删除日志。

## 安全设计（Safety）

- **删除一律走废纸篓**：使用 `NSWorkspace.recycle`，**绝不**执行 `rm` 永久删除，删除可逆。
- **删除前强制确认**：弹窗展示将要移动的文件数量与可释放空间；
  当「整组副本全选」「选中项位于包（.app / .photoslibrary 等）内部」「相似模式可能存在误判」时，
  会额外红色警示。
- **参考文件夹（Reference Folders）**：被标记的目录，其内文件**无法被勾选删除**（需先取消标记）。
- **⌘Z 撤销**：可将最近一次删除的文件从废纸篓移回原位置，并还原结果列表。
- **「保留一个」语义正确**：先把要保留的文件从删除集合中摘除，避免误删唯一副本。
- **默认跳过「包」内部**：`.app` / `.photoslibrary` / `.framework` 等被当作整体跳过，
  从源头避免误删导致 App 损坏或照片库异常。
- **符号链接跳过**：遍历时跳过符号链接，不会因符号链接环陷入死循环，也不会把硬链接误判为重复。

## 关于沙盒（Sandbox）

本工具**未启用 App Sandbox**，这是有意为之：macOS 的沙盒权限模型无法让一个通用去重工具
自由扫描用户指定的任意目录（含桌面 / 文档 / 下载 / 照片等受保护位置），会严重削弱其实用性。
代价是应用本身拥有较大的文件读取范围——但项目**保持零网络代码、零第三方依赖**，
因此即便不靠沙盒，也没有数据外泄的路径。请勿在未来引入任何带网络能力的依赖，
否则将破坏这一安全假设。

## 报告安全漏洞（Reporting a vulnerability）

如果你发现任何安全或隐私问题，请通过 GitHub 的 **Security → Report a vulnerability**（私有漏洞报告）
提交，或直接在该仓库开一个 issue 描述问题。**请勿**公开披露细节，直到修复发布。

由于本工具是本地离线应用，绝大多数「漏洞」场景都涉及误删风险——如你担心某次操作的安全性，
请先确认文件仍在废纸篓中（可从「⌘Z 撤销」恢复）。
