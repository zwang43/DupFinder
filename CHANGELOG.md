# Changelog · DupFinder

本文件记录 DupFinder 的重要变更。版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/) 思路，但本项目为自用工具，仅以「功能里程碑」形式归档。

---

## [0.4.0] — 2026-08-31 · 隐私与安全文档化

- 新增 `SECURITY.md`：声明零网络访问、零遥测、缓存仅含内容哈希（不含路径/文件名）、废纸篓可逆删除等隐私与安全设计。
- 新增 `LICENSE`（MIT，版权人 Zheng Wang 2026）。
- 新增本 `CHANGELOG.md`。
- README 补充「从 GitHub 克隆后如何构建」小节。
- 本地移除 `.build` 编译缓存（301MB，纯可再生成）。

## [0.3.0] — 2026-08-31 · 代码复查修复 + 运行时压力测试

对原始三项需求（重复检测 / 去重清理 / 相似检测）做完整度检阅与压力测试前的代码复查，共发现并修复 **11 项**问题：

**P0（严重）**
- 切换扫描模式（精确 / 相似）时旧模式的勾选项仍可见但不可删（所见非所得）。修复：切换模式时清空两个选择集合并重置文案。

**P1**
- 进度未显示「阶段 x/3」（`ProgressInfo.stageText` 定义但零引用）。修复：进度条改用 `stageText`。
- 重构 `FileRowView` 时丢失「保留这一份（删其余）」右键项。修复：加回该菜单项。

**P2**
- Undo 只恢复磁盘文件、不还原列表。修复：删除前快照 `groups` / `similarGroups` 供撤销还原。
- 相似模式无 inode 去重，硬链接会虚报可节省空间。修复：收集阶段加入 inode 去重。
- 相似模式不统计 `skippedCount`；非图片也误读像素尺寸。修复：补齐统计，读尺寸前先判图片扩展名。

**P3（死代码清理）**
- `HashCache.clear()/.count` 此前无调用。修复：选项区加「清空哈希缓存」按钮。
- 诊断 API（`debugDistance` / `trackBytesRead` / `resetStats`）仅标定用。修复：补注释标明非生产路径。

**运行时压力测试（8/8 全绿）**
- 构造 5 万文件夹具（极深目录 / 空格·中文·emoji 文件名 / 符号链接环 / `chmod 000` 权限受限目录 / `.app` 包 / 两个 50MB 相同大文件）。
- 验证：分组正确、符号链接环不死循环、权限目录优雅跳过、包内部默认不参与、大文件全量哈希路径正确、取消安全返回空。

## [0.2.0] — 2026-08-31 · 计划 C / B / A 迭代

**计划 C · 安全与交互**
- 参考文件夹（盾牌标记、禁止勾选删除）。
- ⌘Z Undo：利用 `NSWorkspace.recycle` 返回的原路径 → 废纸篓映射还原。
- Quick Look（`QLPreviewPanel`）。
- 差异对比（日期 / 像素尺寸）。

**计划 B · 相似图片检测**
- 新增 `SimilarityFinder`：Vision FeaturePrint + Accelerate 距离计算 + 并查集（Union-Find）分组。
- 阈值滑块（默认 0.5，按真实照片标定区间）。

**计划 A · 性能**
- 渐进式哈希（16KB → 1MB → 全量逐级淘汰）+ 哈希缓存（`HashCache`，键为 `设备号:inode:mtime:size`）。
- 设备感知并发。

## [0.1.0] — 2026-08-31 · 初始版本

- 技术栈：SwiftUI 原生 macOS App，SwiftPM，零第三方依赖，仅用 Apple 原生框架。
- 精确去重：`HashEngine`（SHA-256 流式 64KB 分块）、`DuplicateFinder`（遍历 → inode 去重 → 体积初筛 → 并发哈希 → 分组）。
- 安全删除：仅 `NSWorkspace.recycle`（废纸篓，可逆），删除前强制确认 + 三类警示，跳过包内部，符号链接跳过。
- 冒烟测试（`build.sh smoke`，18 项断言全绿）。

---

> 版本号说明：0.x 表示自用阶段，API 与功能仍可能调整。首个稳定公开版将在能力收敛后定为 1.0.0。
